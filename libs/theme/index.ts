/**
 * If we need to access the variables directly in TypeScript, we can add exports here. For example:
 * export const lightBackground = '#ffffff';
 *
 * If any library (like echarts) needs theme variables.
 */
